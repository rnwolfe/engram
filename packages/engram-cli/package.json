{
  "name": "engram-cli",
  "version": "0.3.2",
  "description": "CLI for engram temporal knowledge graph",
  "type": "module",
  "bin": {
    "engram": "dist/cli.js"
  },
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --target bun && bun scripts/copy-assets.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "@engram/engram-web": "workspace:*",
    "@engram/plugin-google-workspace": "workspace:*",
    "commander": "^13.0.0",
    "engram-core": "workspace:*",
    "google-auth-library": "^9.0.0",
    "picocolors": "^1.1.1"
  }
}
